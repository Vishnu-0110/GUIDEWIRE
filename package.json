{
  "name": "gigshield-ai",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered parametric insurance platform for delivery partners",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix ai-service\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ai": "npm run dev --prefix ai-service",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
